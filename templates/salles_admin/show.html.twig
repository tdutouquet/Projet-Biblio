{% extends 'base.html.twig' %}

{% block title %}Salles{% endblock %}

{% block body %}
    <h1>Salles</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ salle.id }}</td>
            </tr>
            <tr>
                <th>Nom</th>
                <td>{{ salle.nom }}</td>
            </tr>
            <tr>
                <th>Capacité</th>
                <td>{{ salle.capacite }}</td>
            </tr>
            <tr>
                <th>Disponibilité</th>
                <td>{{ salle.disponibilite ? 'Yes' : 'No' }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_salles_admin_index') }}">Retour</a>

    <a href="{{ path('app_salles_admin_edit', {'id': salle.id}) }}">Modifier</a>

    {{ include('salles_admin/_delete_form.html.twig') }}
{% endblock %}
