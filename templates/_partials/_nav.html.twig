<nav class="navbar navbar-expand-lg bg-primary" data-bs-theme="dark">
	<div class="container-fluid">
		<a class="navbar-brand" href="{{ path('app_main') }}">MyBiblio</a>
		<button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
			<span class="navbar-toggler-icon"></span>
		</button>
		<div class="collapse navbar-collapse" id="navbarSupportedContent">
			<ul class="navbar-nav me-auto mb-2 mb-lg-0">
				<li class="nav-item">
					<a class="nav-link" href="{{ path("app_livres") }}">Livres</a>
				</li>
				<li class="nav-item">
					<a class="nav-link" href="{{path("app_salles")}}">Salles d'étude</a>
				</li>
			</ul>
			<div class="d-flex">
				{% if app.user %}
					{% if is_granted('ROLE_ADMIN') %}
						<a class="nav-link" href="{{ path('app_main_admin') }}">
							<button type="button" class="btn btn-dark">Administration</button>
						</a>
					{% endif %}
					<a class="nav-link" href="{{ path('app_logout') }}">
						<button type="button" class="btn btn-link">Déconnexion</button>
					</a>
					<a class="nav-link" href="{{ path('app_account') }}">
						<button type="button" class="btn btn-light">Mon compte</button>
					</a>
				{% else %}
					<a class="nav-link" href="{{ path('app_login') }}">
						<button type="button" class="btn btn-link text-decoration-none">Connexion</button>
					</a>
					<a class="nav-link" href="{{ path('app_register') }}">
						<button type="button" class="btn btn-light">Inscription</button>
					</a>
				{% endif %}
				<form action="{{ path('changer_langue') }}" method="get" class="mx-2">
					<select
						name="_locale" onchange="this.form.submit()" class="form-select">
						<!-- Ajout ici d'autres langues -->
						<option value="fr" {% if app.request.locale == 'fr' %} selected {% endif %}>Français</option>
						<option value="en" {% if app.request.locale == 'en' %} selected {% endif %}>English</option>
						<option value="es" {% if app.request.locale == 'es' %} selected {% endif %}>Spanish</option>
					</select>
				</form>
			</div>
		</div>
	</div>
	<div></div>
</nav>
